{
  "name": "lecture-theme-pages",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "npm run sync:sessions && vite",
    "build": "node scripts/build-pages.mjs",
    "preview": "vite preview",
    "sync:sessions": "node scripts/sync-sessions.mjs",
    "build:paste": "node scripts/build-paste.mjs",
    "build:all": "npm run build && npm run build:paste",
    "sync:all": "npm run sync:sessions && npm run build:all",
    "verify:paste": "node scripts/verify-paste.mjs",
    "cms:meta": "node scripts/generate-cms-meta.mjs",
    "export:hero": "node scripts/export-carousel-hero.mjs"
  },
  "dependencies": {
    "react": "^19.1.0",
    "react-dom": "^19.1.0"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.5.2",
    "postcss": "^8.5.6",
    "postcss-prefix-selector": "^2.1.1",
    "puppeteer": "^24.16.0",
    "vite": "^6.3.5",
    "vite-plugin-singlefile": "^2.3.3"
  }
}
